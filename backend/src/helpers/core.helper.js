import User from '@models/mongo/usuarios.model';
import { v4 as uuid } from 'uuid';

export const initCreateRolesAndUser = async () => {
    try {

    } catch (error) {
        console.log(error);
    }
}